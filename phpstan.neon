includes:
    - vendor/larastan/larastan/extension.neon

parameters:

    paths:
        - app
        - bootstrap/app.php
        - config
        - database
        - routes

    level: max

    tmpDir: /tmp/phpstan
